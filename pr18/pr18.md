# Завдання 1

Напиши програму, яка обробляє ділення на нуль (ZeroDivisionError).

```python
try:
    a = int(input("a: "))
    b = int(input("b: "))
    print(a / b)
except ZeroDivisionError:
    print("Ділення на нуль заборонене")
```

# Завдання 2

Зчитай число з клавіатури та оброби ситуацію, коли введено не число (ValueError).

```python
try:
    x = int(input("Введіть число: "))
    print(x)
except ValueError:
    print("Введено не число")
```

# Завдання 3

Перетвори рядок у ціле число з обробкою помилки (ValueError).

```python
try:
    s = "123a"
    n = int(s)
except ValueError:
    print("Неможливо перетворити рядок у число")
```

# Завдання 4

Безпечно отримай елемент списку за індексом, який вводить користувач (IndexError).

```python
lst = [10, 20, 30]
try:
    i = int(input("Індекс: "))
    print(lst[i])
except IndexError:
    print("Невірний індекс")
```

# Завдання 5

Отримай значення зі словника за відсутнім ключем (KeyError).

```python
d = {"a": 1}
try:
    print(d["b"])
except KeyError:
    print("Ключ відсутній")
```

# Завдання 6

Реалізуй калькулятор з обробкою неправильних обчислень (ZeroDivisionError, ValueError).

```python
try:
    a = float(input())
    b = float(input())
    print(a / b)
except ValueError:
    print("Некоректні дані")
except ZeroDivisionError:
    print("Ділення на нуль")
```

# Завдання 7

Напиши програму з блоком else, який виконується лише за відсутності помилок (ValueError).

```python
try:
    x = int(input())
except ValueError:
    print("Помилка")
else:
    print("Успіх:", x)
```

# Завдання 8

Додай блок finally, який виконується незалежно від помилки (Exception).

```python
try:
    int("abc")
except Exception:
    print("Помилка")
finally:
    print("Завершення")
```

# Завдання 9

Відкрий файл за іменем, яке вводить користувач (FileNotFoundError).

```python
try:
    name = input("Ім'я файлу: ")
    f = open(name)
    print(f.read())
    f.close()
except FileNotFoundError:
    print("Файл не знайдено")
```

# Завдання 10

Оброби помилку доступу до файлу без прав читання (PermissionError).

```python
try:
    open("/root/test.txt")
except PermissionError:
    print("Немає прав доступу")
```

# Завдання 11

Оброби кілька винятків в одному except (ValueError, TypeError).

```python
try:
    print(int("a") + "1")
except (ValueError, TypeError):
    print("Помилка типу або значення")
```

# Завдання 12

Перехопи будь-яку помилку та виведи загальне повідомлення (Exception).

```python
try:
    1 / 0
except Exception:
    print("Сталася помилка")
```

# Завдання 13

Виведи власне повідомлення при неправильному типі даних (TypeError).

```python
try:
    print(5 + "a")
except TypeError:
    print("Неправильний тип даних")
```

# Завдання 14

Перевір коректність введених даних для обчислення середнього (ValueError, ZeroDivisionError).

```python
try:
    nums = list(map(int, input().split()))
    print(sum(nums) / len(nums))
except ValueError:
    print("Некоректні дані")
except ZeroDivisionError:
    print("Порожній список")
```

# Завдання 15

Напиши функцію, яка повертає None, якщо сталася помилка перетворення (ValueError).

```python
def to_int(s):
    try:
        return int(s)
    except ValueError:
        return None
```

# Завдання 16

Реалізуй програму, що повторно запитує число до коректного вводу (ValueError).

```python
while True:
    try:
        x = int(input())
        break
    except ValueError:
        print("Спробуй ще")
```

# Завдання 17

Перетвори всі елементи списку у числа з обробкою помилок (ValueError).

```python
lst = ["1", "2", "a"]
res = []
for x in lst:
    try:
        res.append(int(x))
    except ValueError:
        pass
print(res)
```

# Завдання 18

Реалізуй вкладені try / except для введення індексу та доступу до списку (ValueError, IndexError).

```python
lst = [10, 20]
try:
    i = int(input())
    try:
        print(lst[i])
    except IndexError:
        print("Немає такого індексу")
except ValueError:
    print("Не число")
```

# Завдання 19

Розрізни помилки введення користувача та помилки обчислення (ValueError, ZeroDivisionError).

```python
try:
    x = int(input())
    y = int(input())
    print(x / y)
except ValueError:
    print("Помилка введення")
except ZeroDivisionError:
    print("Помилка обчислення")
```

# Завдання 20

Покажи приклад, де програма падає без обробки та працює стабільно з нею (ValueError).

```python
# Без обробки
# int("abc")

# З обробкою
try:
    int("abc")
except ValueError:
    print("Оброблено")
```

# Завдання 21

Обробка KeyError у словнику.

```python
try:
    d = {"x": 1}
    print(d["y"])
except KeyError:
    print("Ключ не знайдено")
```

# Завдання 22

Функція з обробкою ZeroDivisionError.

```python
def divide(a, b):
    try:
        return a / b
    except ZeroDivisionError:
        return None
```

# Завдання 23

Перевірка віку користувача.

```python
try:
    age = int(input())
    if age < 0:
        raise ValueError()
except ValueError:
    print("Некоректний вік")
```

# Завдання 24

Обробка IndexError у списку.

```python
try:
    lst = []
    print(lst[0])
except IndexError:
    print("Список порожній")
```

# Завдання 25

Перехоплення ArithmeticError.

```python
try:
    1 / 0
except ArithmeticError:
    print("Арифметична помилка")
```

# Завдання 26

Обробка ImportError.

```python
try:
    import fake_module
except ImportError:
    print("Модуль не знайдено")
```

# Завдання 27

Обробка PermissionError при записі.

```python
try:
    open("/root/a.txt", "w")
except PermissionError:
    print("Немає доступу")
```

# Завдання 28

Обробка TypeError у функції.

```python
def add(a, b):
    try:
        return a + b
    except TypeError:
        return None
```

# Завдання 29

Перехоплення Exception з повідомленням.

```python
try:
    {}["a"]
except Exception as e:
    print("Помилка:", e)
```

# Завдання 30

Файл + else.

```python
try:
    f = open("a.txt")
except FileNotFoundError:
    print("Файл відсутній")
else:
    print(f.read())
    f.close()
```

# Завдання 31

Власне виключення.

```python
class MyError(Exception):
    pass
```

# Завдання 32

Підняття власного виключення.

```python
try:
    raise MyError("Помилка")
except MyError as e:
    print(e)
```

# Завдання 33

Використання finally з функцією.

```python
def f():
    try:
        return 1 / 0
    finally:
        print("Завершено")
```

# Завдання 34

Перевірка email.

```python
try:
    email = input()
    if "@" not in email:
        raise ValueError()
except ValueError:
    print("Некоректний email")
```

# Завдання 35

Обробка OverflowError.

```python
import math
try:
    math.exp(1000)
except OverflowError:
    print("Переповнення")
```

# Завдання 36

Словник + try/except.

```python
try:
    {}['x']
except KeyError:
    print("Немає ключа")
```

# Завдання 37

Цикл з обробкою ValueError.

```python
while True:
    try:
        int(input())
        break
    except ValueError:
        print("Помилка")
```

# Завдання 38

Ланцюжок виключень.

```python
try:
    int("abc")
except ValueError as e:
    raise RuntimeError("Помилка") from e
```

# Завдання 39

Перехоплення BaseException.

```python
try:
    raise KeyboardInterrupt()
except BaseException:
    print("Перехоплено")
```

# Завдання 40

Повний приклад з усіма блоками.

```python
try:
    x = int(input())
    y = int(input())
    print(x / y)
except Exception as e:
    print("Помилка:", e)
else:
    print("Успіх")
finally:
    print("Кінець")
```
