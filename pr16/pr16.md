## 1. Створити список з 5 цілих чисел

```python
numbers = [1, 2, 3, 4, 5]
print(numbers)
```

## 2. Створити кортеж з рядків

```python
words = ("apple", "banana", "cherry")
print(words)
```

## 3. Вивести перший і останній елемент списку

```python
print(numbers[0], numbers[-1])
```

## 4. Змінити елемент списку за індексом

```python
numbers[2] = 10
print(numbers)
```

## 5. Додати елемент у кінець списку

```python
numbers.append(6)
print(numbers)
```

## 6. Видалити елемент зі списку

```python
numbers.remove(4)
print(numbers)
```

## 7. Отримати зріз списку (2–4 елементи)

```python
print(numbers[1:4])
```

## 8. Порахувати довжину списку

```python
print(len(numbers))
```

## 9. Знайти суму елементів списку

```python
print(sum(numbers))
```

## 10. Знайти мінімум і максимум

```python
print(min(numbers), max(numbers))
```

## 11. Відсортувати список

```python
numbers.sort()
print(numbers)
```

## 12. Розвернути список

```python
numbers.reverse()
print(numbers)
```

## 13. Перевірити наявність елемента

```python
print(10 in numbers)
```

## 14. Об’єднати два списки

```python
list1 = [1, 2]
list2 = [3, 4]
print(list1 + list2)
```

## 15. Створити список квадратів чисел

```python
squares = [x**2 for x in range(1, 6)]
print(squares)
```

## 16. Створити список парних чисел

```python
evens = [x for x in range(10) if x % 2 == 0]
print(evens)
```

## 17. Перетворити список у кортеж

```python
t = tuple(numbers)
print(t)
```

## 18. Перетворити кортеж у список

```python
lst = list(words)
print(lst)
```

## 19. Порахувати кількість входжень елемента

```python
print(numbers.count(10))
```

## 20. Знайти індекс елемента

```python
print(numbers.index(10))
```

## 21. Видалити останній елемент

```python
numbers.pop()
print(numbers)
```

## 22. Очистити список

```python
numbers.clear()
print(numbers)
```

## 23. Створити вкладений список

```python
matrix = [[1, 2], [3, 4]]
print(matrix)
```

## 24. Доступ до елемента вкладеного списку

```python
print(matrix[1][0])
```

## 25. Список довжин слів

```python
lengths = [len(w) for w in words]
print(lengths)
```

## 26. Фільтрація рядків за умовою

```python
long_words = [w for w in words if len(w) > 5]
print(long_words)
```

## 27. Обчислити середнє значення списку

```python
nums = [2, 4, 6, 8]
avg = sum(nums) / len(nums)
print(avg)
```

## 28. Функція для додавання елемента

```python
def add(lst, el):
    lst.append(el)

lst = [1, 2]
add(lst, 3)
print(lst)
```

## 29. Функція для видалення елемента

```python
def remove(lst, el):
    if el in lst:
        lst.remove(el)

remove(lst, 2)
print(lst)
```

## 30. Функція сортування

```python
def sort_list(lst):
    return sorted(lst)

print(sort_list([3, 1, 2]))
```

## 31. Пошук максимального без max()

```python
nums = [3, 7, 2]
m = nums[0]
for x in nums:
    if x > m:
        m = x
print(m)
```

## 32. Перевірка, чи список порожній

```python
print(len(nums) == 0)
```

## 33. Зріз кожного другого елемента

```python
print(nums[::2])
```

## 34. З’єднання рядків зі списку

```python
print(", ".join(words))
```

## 35. Створити список випадкових чисел

```python
import random
rand_nums = [random.randint(1, 10) for _ in range(5)]
print(rand_nums)
```

## 36. Порахувати кількість парних чисел

```python
count = len([x for x in rand_nums if x % 2 == 0])
print(count)
```

## 37. Видалити дублікати зі списку

```python
lst = [1, 2, 2, 3]
unique = list(set(lst))
print(unique)
```

## 38. Відсортувати кортеж

```python
t = (3, 1, 2)
print(tuple(sorted(t)))
```

## 39. Перевірити, чи всі елементи додатні

```python
nums = [1, 2, 3]
print(all(x > 0 for x in nums))
```

## 40. Знайти непарні числа, відсортувати та зберегти у кортеж

```python
nums = [10, 3, 5, 8, 7]
result = tuple(sorted([x for x in nums if x % 2 != 0]))
print(result)
```